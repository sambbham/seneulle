{
  "name": "threadly-cloth-store",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Clothing store with 360° product views, editable from an admin panel (Express + JSON file storage).",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.22.2"
  }
}
